{
  "name": "@mm/core",
  "version": "0.0.1",
  "description": "React components for Magic Mirror.",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && babel lib -d dist",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "lib",
    "dist"
  ],
  "author": "Josh Wilson <joshua.wilson@vanderbilt.edu>",
  "license": "MIT",
  "peerDependencies": {
    "react": "^16.8.0"
  },
  "babel": {
    "plugins": [
      "@babel/plugin-proposal-export-default-from"
    ],
    "presets": [
      "@babel/preset-env",
      "@babel/preset-react"
    ]
  }
}
